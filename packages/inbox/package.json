{
  "name": "@solomonai/inbox",
  "version": "2.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "bun test src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "vitest": "^3.0.7",
    "zod": "^3.23.5",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
