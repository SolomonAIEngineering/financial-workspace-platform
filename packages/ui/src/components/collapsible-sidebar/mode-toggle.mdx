import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import ModeToggle from "./mode-toggle.stories";

<Meta of={ModeToggle} />

# ModeToggle Component

The `ModeToggle` component provides a user interface control for switching between light and dark modes in the application. It typically appears as a button with sun/moon icons that toggles the theme when clicked, enhancing user experience by allowing personalization of the display mode.

## Component Props

<Canvas of={ModeToggle} />

<Controls />

## Usage Example

Here's a basic usage example of the `ModeToggle`:

```tsx
<Canvas>
  <Story name="Default">
    <ModeToggle />
  </Story>
</Canvas>
``` 