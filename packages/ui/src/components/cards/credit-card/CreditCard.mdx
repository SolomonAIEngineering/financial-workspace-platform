import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import CreditCard from "./CreditCard.stories";

<Meta of={CreditCard} />

# CreditCard Component

The `CreditCard` component provides a visual representation of a physical credit card. It displays the card number, expiration date, cardholder name, and card network logo in a format resembling an actual credit card. This component is typically used for card selection interfaces or to visually confirm card details.

## Component Props

<Canvas of={CreditCard} />

<Controls />

## Usage Example

Here's a basic usage example of the `CreditCard`:

```tsx
<Canvas>
  <Story name="Default">
    <CreditCard
      cardNumber="**** **** **** 1234"
      cardholderName="John Smith"
      expiryDate="05/25"
      cardType="visa"
      theme="dark"
      className="w-80"
    />
  </Story>
</Canvas>
``` 