{
  "name": "@solomonai/import",
  "version": "2.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "pnpm test src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "change-case": "^5.4.4",
    "date-fns": "^3.6.0",
    "date-fns-tz": "^3.1.3",
    "tsconfig": "workspace:*",
    "typescript": "^5.7.2",
    "uuid": "^10.0.0",
    "vitest": "^3.0.7"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "tsconfig": "workspace:*"
  }
}
